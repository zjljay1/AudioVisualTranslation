# AudioVisualTranslation (实时音视频翻译系统) 架构设计文档

## 1. 系统概述

AudioVisualTranslation 是一款跨平台的实时音视频翻译工具。该系统旨在捕获用户设备上的音频流（如浏览器网页视频、本地桌面播放器等），通过先进的语音识别 (ASR) 和机器翻译 (MT) 模型，将音频实时转换为目标语言的字幕，并以悬浮窗或网页注入的形式展示给用户。

系统设计核心目标：
- **低延迟**：端到端延迟控制在 3 秒以内。
- **高扩展性**：支持无缝切换底层 ASR 与翻译引擎（云端 API 或 本地大模型）。
- **跨平台一致性**：统一的后端逻辑，服务于浏览器前端与桌面端 UI。

## 2. 系统架构图

系统采用 **C/S 架构 (Client/Server)** 的变体模式，其中 Server 部署在用户本地（Localhost），充当计算和通信中枢；Client 则分为浏览器扩展和桌面 UI 两种形态。

```text
+---------------------------------------------------------+
|                    客户端层 (Clients)                   |
|                                                         |
|  +--------------------+       +----------------------+  |
|  | 浏览器扩展 (Chrome)  |       |  桌面客户端 (PyQt6)   |  |
|  | - Tab Audio 捕获   |       |  - WASAPI 桌面录音    |  |
|  | - DOM 字幕渲染层    |       |  - 置顶透明字幕窗口    |  |
|  +---------+----------+       +----------+-----------+  |
+------------|-----------------------------|--------------+
             | (WebSocket)                 | (WebSocket)
             v Audio Stream / Subtitle     v
+---------------------------------------------------------+
|                  本地服务端层 (Local Backend)           |
|                                                         |
|  +---------------------------------------------------+  |
|  |               WebSocket 总线 (main.py)            |  |
|  +---------------------------------------------------+  |
|          | (Audio Chunks)                  ^            |
|          v                                 | (Text)     |
|  +--------------------+       +----------------------+  |
|  |  语音识别 (ASR)      |------>|  文本翻译 (MT)        |  |
|  |  (asr_engine.py)   | Text  |  (translator.py)     |  |
|  +--------------------+       +----------------------+  |
|          |                                 |            |
|          v                                 v            |
|  [ Whisper API / Local ]       [ GPT-4o / DeepL / Local]|
+---------------------------------------------------------+
```

## 3. 核心模块设计

### 3.1 本地服务端 (Local Backend)
作为系统的“大脑”，负责繁重的任务调度和网络 I/O。
- **通信总线 (`main.py`)**：基于 `websockets` 构建的异步服务器。负责接收来自各个 Client 的二进制音频流，并将识别/翻译后的字幕 JSON 广播给所有已连接的 Client。
- **配置中心 (`config.py`)**：单例模式的配置管理器，支持从 `.env` 和 `config.json` 动态加载模型参数、API Key 和字幕样式。
- **ASR 引擎 (`asr_engine.py`)**：接口化设计。目前已实现基于 `openai` SDK 的 Whisper API 调用。预留了 `faster-whisper` 等本地模型的接入逻辑。
- **翻译引擎 (`translator.py`)**：通过 Prompt Engineering 驱动 LLM (目前默认 GPT-4o-mini) 进行字幕翻译。具备上下文感知潜力。

### 3.2 浏览器扩展层 (Browser Extension)
采用 Manifest V3 标准，解决浏览器端看视频的痛点。
- **音频捕获 (`background.js`)**：利用 `chrome.tabCapture` API 捕获标签页音频，通过 `AudioContext` 解决静音问题。使用 `MediaRecorder` 每 2.5 秒切片生成 WebM 格式的音频块，通过 WebSocket 异步推流。
- **字幕注入 (`content.js` & `styles.css`)**：在目标网页 DOM 中动态注入独立层，实现拖拽、防遮挡、半透明背景等高可用交互。

### 3.3 桌面端 UI 层 (Desktop UI)
基于 `PyQt6` 构建，服务于本地播放器 (PotPlayer) 或原生应用 (B站 UWP)。
- **透明字幕层 (`overlay_window.py`)**：无边框、置顶、鼠标穿透的透明窗口，内置独立的 QThread 运行 WebSocket 客户端，实时渲染字幕。
- **控制面板 (`main_window.py`)**：提供图形化配置界面，控制服务启停。

## 4. 数据流与并发模型

为了实现 **< 3s** 的超低延迟，系统采用了非阻塞的并发流水线模型。

1. **切片**：客户端每隔 `T` 秒 (默认 2.5s) 截断一次音频，发送一个独立的音频文件（WebM / PCM）。
2. **异步流水线**：后端收到 `Chunk N` 后，`main.py` 会使用 `asyncio.create_task()` 创建后台任务。
3. **并行处理**：当 `Chunk N` 正在进行 ASR 和翻译时，客户端仍在录制 `Chunk N+1`。
4. **广播**：`Chunk N` 翻译完成后，立即通过 WebSocket 广播下发，前端收到后执行 DOM/UI 更新。

## 5. 架构演进与未来规划 (Roadmap)

- **VAD (语音活动检测) 引入**：目前采用固定时间切片，容易切断单词。未来将在 Backend 引入 WebRTC VAD 或 Silero VAD，根据静音片段进行智能切片。
- **纯本地化 (Offline Mode)**：完善 `faster-whisper` (ASR) 和 `Ollama / Qwen2.5` (翻译) 的集成，实现完全断网运行，保护用户隐私。
- **跨端同步**：通过同一局域网内的 WebSocket，实现 PC 端播放视频，手机端显示翻译字幕的多屏联动能力。